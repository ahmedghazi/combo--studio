import { SplitImageTextUI } from "@/app/types/schema";
import React from "react";

type Props = {
  input: SplitImageTextUI;
};

const ModuleSplitImageTextUI = (props: Props) => {
  return (
    <section className="module module--split-image-text">
      ModuleSplitImageTextUI
    </section>
  );
};

export default ModuleSplitImageTextUI;
