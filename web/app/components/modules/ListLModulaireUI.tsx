import { ListLModulaireUI } from "@/app/types/schema";
import React from "react";

type Props = {
  input: ListLModulaireUI;
};

const ModuleListLModulaireUI = (props: Props) => {
  return (
    <section className="module module--list-modulaire">
      ModuleListLModulaireUI
    </section>
  );
};

export default ModuleListLModulaireUI;
