import { ListCardImageTextUI } from "@/app/types/schema";
import React from "react";

type Props = {
  input: ListCardImageTextUI;
};

const ModuleListCardImageTextUI = (props: Props) => {
  return (
    <section className="module module--list-card-image-text">
      ModuleListCardImageTextUI
    </section>
  );
};

export default ModuleListCardImageTextUI;
